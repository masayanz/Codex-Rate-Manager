# 検証記録

2026年9月9日、Windows 11 / Python 3.13 / Codex CLI 0.153.4 で確認。

## 2026年9月10日 スキン・位置管理改修

- `scripts/build.ps1` 成功。EXEだけをソースとは別の作業フォルダーへコピーし、標準3スキンすべてで起動・画面描画・画面外位置からの復元・正常終了を確認。外部スキンフォルダーなしで動作。
- EXEのリソースを解析し、3スキン、ICO、通知用PNG、9サイズのWindowsアイコンリソースが含まれることを確認。
- 自動テスト：77 passed, 1 skipped（offscreenのトレイ格納）。
- Windowsで3スキンの選択直後の本体/トレイ反映、保存・取消、設定ファイルへの保存を確認。
- Qtスケール1.0 / 1.25 / 1.5 / 1.75で画面を保存し、表示と固定ボタンを確認。175%相当では内容をスクロール表示。
- 接続中の3台のモニター（負座標や上下配置を含む）で、タイトルバーを含む外枠がavailableGeometry内へ補正されることを実ウィンドウで確認。
- 保存先モニター消失を模擬し、プライマリ画面への復帰を確認。実際のケーブル抜き差し・OSのDPI設定変更は未実施。
- 小さいウィンドウで本文だけスクロールし、下部ボタンが表示されることを確認。
- 保存済みMinimal Darkと外枠の座標・サイズが、別プロセスで再起動した後も同一に復元されることを確認。
- 移動位置の保存、旧configへの項目追加、既存通知設定と未知の設定項目の維持をテスト。
- 300msアニメーションの停止、無効化時の即時反映をテスト。
- ICO内の16/20/24/32/40/48/64/128/256pxの9画像を検証。Windowsネイティブの大小アイコンハンドルを確認。
- タスクバー/Alt+TabのShell表示を目視での確認は未実施。QApplicationとMainWindowへの専用ICO設定、AppUserModelID設定を実装。

## サイドパネル・onefile更新の検証

- 自動テスト：60 passed, 1 skipped。スキップはoffscreen環境でのトレイ格納テスト。
- 16pxの二重リング／上下バーで、100/100、50/50、20/80、0/80、80/0、切断状態の画像が区別されることを検証。
- 外観設定の保存・実行中の反映、トレイの両枠表示、切断時の最終成功表示を検証。
- 新UIのWindows描画を確認。Qtのスケール指定1.0 / 1.25 / 1.5で画像を保存し、文字と主要ボタンの欠けがないことを確認。OS設定変更や異なるDPIのモニター間移動は未実施。
- `scripts/build.ps1` が依存確認、テスト、旧配布物退避、onefile生成まで成功。`dist` は `CodexRateManager.exe` の1ファイルのみ。
- onefile EXEのみを別フォルダーへコピーして単体起動し、実Codexの両枠取得、GUI・トレイ表示、DB保存、Windows ToastのOS登録、終了コード0を確認。
- 必須枠の欠落した応答が利用可能判定されず、自動再接続へ移ることを検証。

以下は更新前の既存実装に対する検証記録です。

## 自動テスト（更新前）

`.venv\Scripts\python.exe -m pytest -q`：**56 passed, 1 skipped**。

- 残量計算、期間による枠の識別、必須枠欠落時の判定
- 両枠の上限、週間上限が残る場合の復帰抑止、低残量状態への復帰
- リセット予定時刻を過ぎても上限解除を推測しないこと
- 再起動後の状態復元・通知重複防止、アカウント変更時の状態分離
- 監視間隔、切断時の30/60/120秒バックオフ、復帰時の即時取得要求
- JSON-RPC初期化・更新通知・タイムアウト・EOF・自身の子プロセス終了
- Discord HTTP 204/400/429/500、再送待機、キャンセル、URL検証
- Windows通知失敗時もDiscord通知処理を継続
- DPAPIの暗号化・復号（Windows実APIを使用）
- SQLite履歴・通知履歴、設定保存、GUIのモック状態変更
- 自動起動のHKCU登録内容（レジストリAPIをモック。実際の設定は変更しない）

skipはoffscreen環境にシステムトレイがないための1件。同じ機能を下記のWindows実画面で別途確認した。

## Windows実画面・生成物

- ソース実行とPyInstallerのonedir EXEから、実Codex App Serverへ接続
- 5時間・週間の残量、リセット時刻、カウントダウンを表示
- メイン画面、設定画面、履歴画面、診断画面の描画を確認
- 設定変更を保存し、実行中のアプリへ反映
- ×ボタンでトレイ格納、二重起動したプロセスは終了し既存画面を再表示
- モックの5時間上限→週間上限→両枠利用可能で、最後だけ復帰通知を発生
- ソース実行・EXEの両方からWindows Toast登録に成功し通知履歴を保存
- 実アプリ終了時に、自身のApp Serverプロセスが終了することを確認
- `scripts\build.ps1` で依存確認、テスト、EXE生成に成功

検証用データと画像は `test-artifacts` 以下へ分離し、Git管理から除外した。

## 未確認の実環境条件

- 実Discordへの着信：Webhook URL未提供のため未実施。設定画面からテスト送信可能
- 実際のWindowsログイン時の自動起動：利用者の起動設定は変更していない
- PCを実際にスリープさせた後の復帰：イベント処理と取得要求をテストしたが、実スリープは未実施
- 実アカウントでレート上限に達してからリセットされるまでの長時間監視：モックと回帰テストで検証

Windows通知の成功はOSへの登録を意味する。画面への表示は集中モードやWindowsの通知設定に依存する。
